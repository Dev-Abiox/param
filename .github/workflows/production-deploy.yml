name: Production Deploy

on:
  push:
    branches: [ main, master ]

env:
  BACKEND_IMAGE: ghcr.io/dev-abiox/param
  FRONTEND_IMAGE: ghcr.io/dev-abiox/param-frontend

jobs:
  test-backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: clinomic_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: |
          cd backend_v3
          pip install setuptools
          pip install -r requirements.txt
      - name: Run backend tests with coverage
        env:
          DJANGO_SECRET_KEY: ci-secret-key-not-for-production
          JWT_SECRET_KEY: ci-jwt-secret-key-not-for-production
          JWT_REFRESH_SECRET_KEY: ci-jwt-refresh-secret-not-for-production
          AUDIT_SIGNING_KEY: ci-audit-signing-key-not-for-production-at-all
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_DB: clinomic_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          REDIS_URL: redis://localhost:6379/0
          CACHE_REDIS_URL: redis://localhost:6379/1
          CELERY_BROKER_URL: redis://localhost:6379/0
          CHANNELS_REDIS_URL: redis://localhost:6379/2
          APP_ENV: testing
          DEBUG: "False"
        run: |
          cd backend_v3
          pytest --cov=apps --cov-config=.coveragerc --cov-report=term-missing --cov-fail-under=60 -x -q

  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Run frontend tests
        run: |
          cd frontend
          npm ci --legacy-peer-deps
          CI=true npm test -- --watchAll=false

  build:
    needs: [test-backend, test-frontend]
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend_v3
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Build frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}:latest
            ${{ env.FRONTEND_IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Scan backend image for vulnerabilities
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.BACKEND_IMAGE }}:${{ github.sha }}
          trivyignores: backend_v3/.trivyignore
          exit-code: 1
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          timeout: 10m

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      - name: Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -t rsa -H ${{ secrets.HOST_IP }} >> ~/.ssh/known_hosts
          # Server only supports ssh-rsa — configure client to accept it
          cat >> ~/.ssh/config << SSHEOF
          Host ${{ secrets.HOST_IP }}
            HostKeyAlgorithms +ssh-rsa
            PubkeyAcceptedAlgorithms +ssh-rsa
          SSHEOF
      - name: Copy config files to server
        run: |
          SERVER="deploy@${{ secrets.HOST_IP }}"
          ssh $SERVER "mkdir -p /opt/clinomic-b12-platform/ml/models"
          ssh $SERVER 'docker run --rm -v /opt/clinomic-b12-platform:/host alpine sh -c "rm -rf /host/monitoring && mkdir -p /host/monitoring/grafana/provisioning/datasources /host/monitoring/grafana/provisioning/dashboards && chmod -R 777 /host/monitoring"'
          scp docker-compose.prod.yml $SERVER:/opt/clinomic-b12-platform/docker-compose.prod.yml
          scp nginx.prod.conf $SERVER:/opt/clinomic-b12-platform/nginx.prod.conf
          scp -r backend_v3/ml/models/ $SERVER:/opt/clinomic-b12-platform/ml/
          scp monitoring/prometheus.yml $SERVER:/opt/clinomic-b12-platform/monitoring/prometheus.yml
          scp -r monitoring/grafana/provisioning/ $SERVER:/opt/clinomic-b12-platform/monitoring/grafana/provisioning/
      - name: Deploy to Production
        run: |
          ssh deploy@${{ secrets.HOST_IP }} << 'EOF'

          set -e

          BACKEND_IMAGE="ghcr.io/dev-abiox/param"
          FRONTEND_IMAGE="ghcr.io/dev-abiox/param-frontend"
          BASE=/opt/clinomic-b12-platform
          COMPOSE="$BASE/docker-compose.prod.yml"

          echo "=== VALIDATING DEPLOYMENT ==="
          [ -f "$COMPOSE" ] || { echo "ERROR: $COMPOSE not found"; exit 1; }
          [ -f "$BASE/nginx.prod.conf" ] || { echo "ERROR: nginx.prod.conf not found"; exit 1; }

          echo "=== ENSURING SHARED DOCKER NETWORK ==="
          docker network create clinomic_network 2>/dev/null || true

          echo "=== SAVING CURRENT IMAGES FOR ROLLBACK ==="
          cd $BASE
          PREV_BACKEND=$(docker inspect --format='{{.Id}}' $BACKEND_IMAGE:latest 2>/dev/null || echo "")
          PREV_FRONTEND=$(docker inspect --format='{{.Id}}' $FRONTEND_IMAGE:latest 2>/dev/null || echo "")
          [ -n "$PREV_BACKEND" ] && docker tag $BACKEND_IMAGE:latest $BACKEND_IMAGE:previous || true
          [ -n "$PREV_FRONTEND" ] && docker tag $FRONTEND_IMAGE:latest $FRONTEND_IMAGE:previous || true

          echo "=== PULLING NEW IMAGES ==="
          docker-compose -f $COMPOSE pull

          echo "=== RECREATING CONTAINERS ==="
          docker-compose -f $COMPOSE up -d --force-recreate

          echo "=== WARMING UP SERVICES ==="
          sleep 30

          echo "=== HEALTH CHECK ==="
          if curl -f https://clinomiclabs.com/api/health/live; then
            echo "✓ Health check passed"
          else
            echo "✗ Health check failed — ROLLING BACK to previous images"
            [ -n "$PREV_BACKEND" ] && docker tag $BACKEND_IMAGE:previous $BACKEND_IMAGE:latest || true
            [ -n "$PREV_FRONTEND" ] && docker tag $FRONTEND_IMAGE:previous $FRONTEND_IMAGE:latest || true
            docker-compose -f $COMPOSE up -d --force-recreate
            echo "Rollback complete."
            exit 1
          fi

          echo "✅ DEPLOYMENT COMPLETED SUCCESSFULLY"

          EOF
